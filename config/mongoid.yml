development:
  sessions:
    default:
      database: oompa_development
      hosts:
        - localhost:27017
test:
  sessions:
    default:
      database: oompa_test
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: oompa_production
      hosts:
        - <%= ENV['MONGODB_PORT_27017_TCP_ADDR'] || 'localhost' %>:27017
